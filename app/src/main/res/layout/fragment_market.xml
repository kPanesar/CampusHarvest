<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/market_fragment">

    <!--List Header-->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/market_list_padding"
        android:paddingBottom="@dimen/market_list_padding"
        android:orientation="horizontal"
        android:background="@color/accent">

        <TextView
            android:id="@+id/market_list_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textColor="@android:color/white"
            android:layout_gravity="center_vertical"
            android:layout_weight="1.2"
            android:gravity="left"
            android:text="Item Name"/>

        <TextView
            android:id="@+id/market_list_price"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:textColor="@android:color/white"
            android:layout_weight="1"
            android:gravity="center"
            android:text="Price"/>

        <TextView
            android:id="@+id/market_list_category"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:textColor="@android:color/white"
            android:text="Category"
            android:layout_weight="1"
            android:gravity="right"/>

    </LinearLayout>

    <!-- The navigation drawer (sliding menu) -->
    <ListView
        android:id="@+id/lv_market_list"
        android:layout_width="fill_parent"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:choiceMode="singleChoice"
        android:divider="@null"
        android:dividerHeight="0dp"
        android:background="@android:color/white"/>

</LinearLayout>